-- Jupyter-like kit
local M = {
    'luk400/vim-jukit'
}

function M.config() 
  -- nvim.builtin.project.patterns = { ">projects", ".jukit", ".git" } -- defaults include other vcss, makefile, package.json
end

return M
